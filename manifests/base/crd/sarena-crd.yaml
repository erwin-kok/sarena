---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: addresspools.sarena.erwinkok.org
spec:
  group: sarena.erwinkok.org
  names:
    kind: AddressPool
    plural: addresspools
    singular: addresspool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: CIDR of Address pool
      jsonPath: .spec.cidr
      name: CIDR
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for AddressPoolSpec via `CustomResource`
        properties:
          spec:
            properties:
              cidr:
                type: string
            required:
            - cidr
            type: object
          status:
            nullable: true
            type: object
        required:
        - spec
        title: AddressPool
        type: object
    served: true
    storage: true
    subresources:
      status: {}
